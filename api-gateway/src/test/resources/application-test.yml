spring:
  application:
    name: api-gateway
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 0

eureka:
  client:
    enabled: false

logging:
  level:
    com.kawashreh.ecommerce: DEBUG
