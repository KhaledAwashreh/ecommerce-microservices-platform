# IDE Profile — run frontend-service in IDE, everything else in Docker
spring:
  zipkin:
    base-url: http://localhost:9411

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
