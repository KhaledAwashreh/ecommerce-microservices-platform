spring:
  application:
    name: frontend-service
  datasource:
    url: jdbc:postgresql://localhost:5432/frontenddb
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
server:
  port: 0

logging:
  level:
    com.kawashreh.ecommerce: DEBUG
