# IDE Profile — run user-service in IDE, everything else in Docker
spring:
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5433/userdb
    username: postgres
    password: test1234

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
