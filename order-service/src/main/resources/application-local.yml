# ===============================
# Order Service - Local Profile
# For running from IDE without Docker
# ===============================

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/orderdb
    username: postgres
    password: test1234
  data:
    redis:
      host: localhost
      port: 6379

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
