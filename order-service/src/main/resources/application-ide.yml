# IDE Profile — run order-service in IDE, everything else in Docker
spring:
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5435/orderdb
    username: postgres
    password: test1234
  cloud:
    openfeign:
      client:
        config:
          # order-service calls all upstream services through the gateway
          user-service:
            url: http://localhost:8765
          product-service:
            url: http://localhost:8765
          payment-service:
            url: http://localhost:8765

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
