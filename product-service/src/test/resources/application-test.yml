spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/productdb
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 0

# Test-only signing key. Production requires JWT_SECRET (no committed default) —
# see application.yml. This value is not used outside the `test` profile.
jwt:
  secret: dGVzdC1vbmx5LWp3dC1zaWduaW5nLXNlY3JldC1kby1ub3QtdXNlLWluLXByb2Q=

logging:
  level:
    com.kawashreh.ecommerce: DEBUG
